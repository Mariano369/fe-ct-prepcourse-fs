function obtenerMayor(x, y) {
  // "x" e "y" son números enteros.
  // Retorna el número más grande.
  // Si son iguales, retornar cualquiera de los dos.
  // Tu código:

var resultado = Math.max (x,y) 
return resultado
}

//   if (x > y) return x
//  else return y
// }

// return Math.max (x , y)
// }

module.exports = obtenerMayor;
