// Definición de la función totalDeProductos

function totalDeProductos(producto1, producto2) {
    var suma = producto1 + producto2;
    return suma;
}

// Llamada a la función con dos productos y allacenamiento

var resultado = totalDeProductos(10, 20);

// Imprimir el resultado en consola 

console.log("El total del resultado es: " + resultado);


//ACA ABAJO MUESTRO COMO LLAMO Y GUARDO UNA FUNCION, PERO NO LA IMPRIMO EN PANTALLA

// Definición de la función totalDeProductos
function totalDeProductos(producto1, producto2) {
    var suma = producto1 + producto2;
    return suma;
}

// Llamada a la función y almacenamiento del resultado

var resultado = totalDeProductos(10, 20);



function totalDeProductos(producto1,producto2) {
var suma = producto1 + producto2;
return suma;
}

var resultado = totalDeProductos (20,40);

console.log ("resultado de la funcion: ",resultado);




                                CONDICIONALES 
var mariano = 17  
var carlos = 16

function controlarEdadMariano () {

    if (mariano >= 18) {
return ("Mariano es mayor de edad")
    } else 
return ("Mariano es menor de edad")
}

function controlarEdadCarlos () {
    if (carlos >= 18) {
return ("Carlos es mayor de edad")
    } else 
return ("Carlos es menor de edad")
}

//otro ej diferente pero similar

function controlarEdadPersonas () {

    if (mariano >= 18) {
return ("Mariano es mayor de edad")
    } else if (carlos >= 18) {
return ("Carlos es mayor de edad")
    } else 
return ("Ninguna de las dos personas son mayores de edad")
}
 //mostrar  el resultado de las funciones pero no se guarda , ya esta guardado  al hacer return y tambien guarde los resultados de cada funcion en variables al final 

console.log((controlarEdadMariano()))
console.log((controlarEdadCarlos()))
console.log((controlarEdadPersonas()))


 // guardar el resultado de las funciones 

 var resultadoEdadMariano = controlarEdadCarlos ()
var resultadoEdadCarlos = controlarEdadCarlos ()
var resultadoComparacionDeEdadesEntreCarlosMariano = controlarEdadMariano ()

                                 // OBJETOS

// OBJETO PADRE {} 

// * TIENE TODO EL CONTENIDO DE JAVASCRIPT
// * ENTREGA PROPIEDADES A OTROS OBJETOS

// OBJETOS QUE RECIBEN PROPIEDADES DEL OBJETO PADRE - NACEN DEL OJETO PADRE

// STRING, NUMBER, BOOLEANO, OBJECT, ARRAY -->  FUNCIONALIDADES  (esto es imp) - TIPOS DE DATOS - METODS - SE DINTINGUEN EN VIOLETA A DIF DE LAS AZULES QUE SE IDENTIFICAN COMO PROPIEDADES, NO COMO FUNCIONALIDADES.

// OTROS OBJETOS QUE RECIBEN PROPIEDADES DEL OBJETO PADRE - NACEN DEL OJETO PADRE

// CONSOLE. (LOG, ERROR, ETC)
// MATH (POW, ROUND, FLOOR, CEIL, ETC)

// var ejemploMathRound = Math.round(10.52)
// console.log(ejemploMathRound)

// var ejemploMathFloor = Math.floor(10.52)
// console.log(ejemploMathFloor)




























